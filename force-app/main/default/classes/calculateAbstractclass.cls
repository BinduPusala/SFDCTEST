public class calculateAbstractclass extends AbstractClass {
    
    protected override void calculateprice(Integer count){
     system.debug('final amount based on quantity' +amount*count);
        }
}